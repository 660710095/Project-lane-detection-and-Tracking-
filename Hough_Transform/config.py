import numpy as np

# ===========================================================================
# Gaussian Blur
# ===========================================================================
BLUR_KERNEL_SIZE = 5

# ===========================================================================
# Canny Edge Detection
# ลด threshold เพื่อจับ edge ของเส้นซีด
# ===========================================================================
CANNY_THRESHOLD1 = 30   # ลดจาก 50 → จับ edge อ่อนได้
CANNY_THRESHOLD2 = 100  # ลดจาก 150 → จับ edge อ่อนได้

# ===========================================================================
# ROI ซ้าย
# ===========================================================================
LEFT_ROI_TOP_Y        = 0.75
LEFT_ROI_TOP_LEFT_X   = 0.15
LEFT_ROI_TOP_RIGHT_X  = 0.50
LEFT_ROI_BOT_LEFT_X   = 0.10
LEFT_ROI_BOT_RIGHT_X  = 0.50

# ===========================================================================
# ROI ขวา
# ===========================================================================
RIGHT_ROI_TOP_Y       = 0.75
RIGHT_ROI_TOP_LEFT_X  = 0.50
RIGHT_ROI_TOP_RIGHT_X = 0.90
RIGHT_ROI_BOT_LEFT_X  = 0.50
RIGHT_ROI_BOT_RIGHT_X = 1.00

# ===========================================================================
# Hough Lines
# ลด threshold + min_len เพื่อจับเส้นปะสั้นได้
# ===========================================================================
HOUGH_RHO          = 1
HOUGH_THETA        = np.pi / 180
HOUGH_THRESHOLD    = 30   # ลดจาก 50 → ต้องการ vote น้อยลง
HOUGH_MIN_LINE_LEN = 40   # ลดจาก 80 → จับเส้นปะสั้นได้
HOUGH_MAX_LINE_GAP = 250  # เพิ่มจาก 200 → ต่อเส้นปะที่มีช่องว่างยาวได้

# ===========================================================================
# Slope filter
# ===========================================================================
SLOPE_MIN = 0.4
SLOPE_MAX = 2.5

# ===========================================================================
# RANSAC
# ===========================================================================
RANSAC_ITERATIONS  = 50
RANSAC_THRESHOLD   = 15   # เพิ่มจาก 12 → inlier range กว้างขึ้น เส้นปะไม่ตรงทั้งหมด

# ===========================================================================
# Smoother (Kalman)
# ===========================================================================
SMOOTH_ALPHA       = 0.65
SMOOTH_MAX_JUMP    = 120
SMOOTH_CONF_MIN    = 0.2  # ลดจาก 0.3 → ยอมรับ confidence ต่ำกว่าเดิม

# ===========================================================================
# Lane Keep Assist
# ===========================================================================
DRIFT_THRESHOLD     = 50
OVERLAY_ALPHA       = 0.4
DRIFT_THRESHOLD_ON  = 60
DRIFT_THRESHOLD_OFF = 40

# ชดเชยตำแหน่งกล้องที่ไม่ได้อยู่กึ่งกลางรถ
CAMERA_OFFSET = -89
