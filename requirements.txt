# ==========================================
# Project CS29: Lane Detection & Tracking
# ==========================================

# --- 1. Image Processing & Core Libraries (ส่วนที่ทำเสร็จแล้ว) ---
opencv-python>=4.8.0
numpy>=1.24.0
matplotlib>=3.7.0

# --- 2. Machine Learning (ส่วนที่คุณมโนรินทร์กำลังจะทำ) ---
# เลือกเอาคอมเมนต์ (#) ออกตาม Framework ที่คุณมโนรินทร์ตัดสินใจเลือกใช้ครับ
# scikit-learn>=1.3.0
# torch>=2.0.0        # สำหรับ PyTorch
# torchvision>=0.15.0
# tensorflow>=2.14.0  # สำหรับ TensorFlow

# --- 3. Web Application (ส่วนที่คุณปั๊มจะนำไปพัฒนาต่อ) ---
# ตัวอย่างไลบรารีสำหรับทำระบบอัปโหลดวิดีโอและแสดงผล (เลือกเปิดใช้ตามที่ตกลงกัน)
# fastapi>=0.103.0
# uvicorn>=0.23.0
# python-multipart    # จำเป็นสำหรับให้ Web App รับไฟล์วิดีโอจากผู้ใช้
# flask>=2.3.0        # หรือถ้าถนัด Flask มากกว่าก็ใช้ตัวนี้ครับ