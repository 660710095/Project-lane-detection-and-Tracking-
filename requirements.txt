# ==========================================
# Project CS29: Lane Detection & Tracking
# ==========================================

# --- 1. Image Processing & Core Libraries (ส่วนที่ทำเสร็จแล้ว) ---
opencv-python>=4.8.0
numpy>=1.24.0
matplotlib>=3.7.0

# --- 2. Machine Learning (ส่วนสอน AI ด้วย YOLO) ---
ultralytics>=8.1.0      # อาวุธหลักสำหรับเทรนโมเดล YOLOv8, v11, v12
torch>=2.0.0            # เครื่องยนต์หลัก (PyTorch) สำหรับให้การ์ดจอคำนวณ AI
torchvision>=0.15.0     # เครื่องมือช่วยจัดการรูปภาพของ PyTorch
# labelme>=5.4.1        # เอาคอมเมนต์ออกถ้าอนาคตอยากใช้โปรแกรมวาด Label เอง

# --- 3. Web Application (ส่วนที่คุณปั๊มจะนำไปพัฒนาต่อ) ---
# แนะนำชุด FastAPI (ทำงานเร็วกว่าและรองรับ AI ได้ดีกว่า Flask)
fastapi>=0.103.0
uvicorn>=0.23.0
python-multipart        # จำเป็นสำหรับให้ Web App รับไฟล์วิดีโอจากผู้ใช้
# flask>=2.3.0          # (เผื่อเลือก) ถ้าถนัด Flask มากกว่าก็สลับมาใช้ตัวนี้แทน